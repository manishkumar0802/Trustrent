{
  "name": "@trustrent/blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "TrustRent Soroban client layer. Phase 1: typed stub + network config. Phase 2: wired to @stellar/stellar-sdk contract clients.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check src",
    "test": "echo \"no tests yet — see packages/blockchain/README.md\" && exit 0"
  },
  "dependencies": {
    "@trustrent/types": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
