{
  "name": "@trustrent/shared",
  "version": "0.1.0",
  "private": true,
  "description": "TrustRent shared runtime helpers: lifecycle constants, formatting, contract event labels.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check src",
    "test": "vitest run"
  },
  "dependencies": {
    "@trustrent/types": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
