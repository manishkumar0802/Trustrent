{
  "name": "@trustrent/types",
  "version": "0.1.0",
  "private": true,
  "description": "TrustRent shared domain types — mirrors the Soroban contract state machine and event catalog.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check src",
    "test": "echo \"no tests yet — see packages/types/README.md\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
